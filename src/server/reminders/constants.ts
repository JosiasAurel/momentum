export const REMINDER_OFFSETS_MINUTES = [72 * 60, 24 * 60, 6 * 60, 60, 0] as const;

export const MAX_TASKS_PER_DAY = 5;
export const MAX_RESCHEDULE_SEARCH_DAYS = 180;
export const DEFAULT_WORKER_BATCH_SIZE = 100;
